
# 环境
Windows 11 25H2
mingw64
cmake 4.3.1

配置




# 渲染器
PBRT-v3


### PBRT-v3 攻克 Top 4 - Top 1 实战地图

| 难度    | 优化方向                  | PBRT-v3 核心源码位置                                           | 你需要魔改/研究的具体函数                          | 大作业拿分点                                                           |
| :---- | :-------------------- | :------------------------------------------------------- | :------------------------------------- | :--------------------------------------------------------------- |
| Top 4 | 基础方差优化  <br>(俄罗斯轮盘赌)  | `src/core/interaction.h`  <br>`src/integrators/path.cpp` | `PathIntegrator::Li()` 中的反弹循环          | 在递归处加入存活概率判断与权重补偿，对比固定深度截断的有偏误差。                                 |
| Top 3 | 最优样本分配  <br>(自适应采样)   | `src/samplers/adaptive.cpp`                              | `AdaptiveSampler::Get2DSample()`       | PBRT 自带自适应采样器！你只需调整方差阈值参数，即可跑出完美的资源分配热力图。                        |
| Top 2 | 空间搜索优化  <br>(SAH-BVH) | `src/accelerators/bvh.cpp`                               | `BVHAccel::recursiveBuild()`           |                                                                  |
| Top 1 | 多重重要性采样  <br>(MIS)    | `src/integrators/directlighting.cpp`                     | `DirectLightingIntegrator::SampleLd()` | 清晰分离了光源采样与 BSDF 采样 PDF，可直接修改 Balance/Power Heuristic 权重公式进行消融实验。 |



# 成果与数据
### 第一类：定性可视化（渲染图像对比）—— 负责“吸引眼球”

这是为了让非图形学背景的人（或者老师）一眼看出你的优化“有效果”。

#### 1. 局部放大对比图 (Crop & Zoom)

- **怎么展示**：不要只放两张完整的全景图，老师看不清噪点。要在图中画个红框，然后把红框里的区域**放大**并排展示。
- **展示组合**：
    - **图A (Baseline)**: 100 SPP (每像素采样数)，满屏噪点。
    - **图B (你的优化)**: 100 SPP，画面干净。
    - **图C (Ground Truth)**: 10000 SPP 的无噪参考图。
- **代码库支持**：不需要写代码。直接用你的渲染器输出 PNG 图片，然后用 **PPT、Photoshop 或 系统自带截图工具** 裁剪排版即可。

---

### 第二类：定量可视化（核心数据图表）—— 负责“拿高分” 🌟

这是大作业的**灵魂**。你需要把渲染过程中记录的日志数据画成学术折线图。

#### 1. 收敛曲线 (Convergence Curve)

- **证明什么**：证明你的优化算法“收敛得更快”。
- **X轴**：迭代次数 或 每像素样本数 (SPP) 或 渲染耗时 (秒)。
- **Y轴**：目标函数值（通常是 MSE 均方误差 或 RMSE）。**注意：Y轴通常要用对数坐标 (Log Scale)，因为误差下降是非线性的。**
- **怎么画**：你的渲染器在跑的时候，每增加 10 个 SPP，就计算一下当前图和参考图的 MSE，存入 CSV。
- **代码库支持**：`matplotlib`
- **极简代码示例**：


#### 2. 复杂度缩放图 (Scaling Plot) - 针对路线A(空间优化/BVH)

- **证明什么**：证明你的算法把时间复杂度从 �(�)O(N) 降到了 �(log⁡�)O(logN) 。
- **X轴**：场景中的物体数量 (N) [例如：1千, 1万, 10万, 100万]。
- **Y轴**：单帧渲染耗时 (秒)。
- **展示效果**：Baseline 是一条直线上升的斜线，而你的 BVH 是一条平缓的对数曲线。
- **代码库**：同上，使用 `matplotlib` 的 `plt.plot()`。

---

### 第三类：高阶可视化（降维打击图）—— 负责“体现专业度” 🚀

如果你想让老师觉得“这学生真的懂最优化”，加上这两种图。

#### 1. 误差热力图 (Error Heatmap)

- **证明什么**：直观展示误差（方差）在空间上的分布。证明你的优化算法把计算资源（样本）集中在了最难算的地方（自适应采样）。
- **怎么画**：用代码计算“优化后图片”与“参考图”每一个像素的差值，然后用伪彩图（如 `jet` 或 `magma` 色谱）把差值画出来。红色代表误差大，蓝色代表误差小。
- **代码库支持**：`OpenCV` (算差值) + `matplotlib.pyplot.imshow`


#### 2. 2D 采样点分布图 (Sampling Pattern) - 针对路线B(积分优化)

- **证明什么**：展示你生成的随机数质量。证明“低差异序列（如 Sobol/Halton）”或“分层采样”比“纯随机采样”在空间上分布得更均匀。
- **怎么画**：在 2D 坐标系里散点图打出 100 个采样点。
- **代码库支持**：`matplotlib.pyplot.scatter`

---

### 第四类：工程可视化（性能剖析图）—— 负责“严谨性”

最优化往往是有代价的（Trade-off）。展示你清楚优化的代价在哪里。

#### 1. 耗时占比堆叠柱状图 (Stacked Bar Chart)

- **证明什么**：说明“建树/预处理”虽然花时间，但“光线遍历/渲染”省下了更多的时间。
- **怎么画**：一个柱子代表 Baseline（全是渲染时间）；另一个柱子代表 你的算法（底部一小块是“建树时间”，上面一大块是“渲染时间”）。总高度明显变矮。
- **代码库支持**：`matplotlib.pyplot.bar` 并设置 `bottom` 参数。

| PPT 页码 | 页面内容建议 | 演讲话术参考 (时间分配) |
| :--- | :--- | :--- |
| Page 1 | 封面<br>标题、姓名、学号 | “各位老师/同学好，我今天汇报的题目是《光线追踪中XX问题的优化算法研究》。” (0.5 min) |
| Page 2 | 背景与痛点<br>左边放一张充满噪点的光追图，右边放一个耗时很长的进度条。 | “光线追踪能生成逼真的图像，但它面临两大瓶颈：一是射线求交慢，二是积分噪点多。这本质上是一个计算资源受限下的最优化问题。” (1 min) |
| Page 3 | 数学建模 🌟<br>清晰列出：决策变量、目标函数、约束条件。 | “为了用最优化的视角解决它，我建立了如下模型。我的决策变量是X，我的目标是最小化Y（代价/方差），同时满足Z的约束条件。” (1.5 min) |
| Page 4 | Baseline 分析<br>写一下朴素算法的复杂度公式  $ O(N) $  或 方差公式。 | “如果不做优化，朴素算法的复杂度是  $ O(N) $  / 方差极高。随着场景变大/样本减少，目标函数值会急剧恶化。” (1 min) |
| Page 5 | 我的优化算法 🌟<br>核心公式 + 算法伪代码 (高亮关键步骤)。 | “为此，我引入了XX优化算法。核心思想是通过XX启发式规则/拉格朗日乘子法，在每一步迭代中寻找局部/全局最优解。这是算法的伪代码……” (2 min) |
| Page 6 | 实验设置与视觉对比<br>并排展示 Baseline 和 优化后 的渲染图（标注好SPP和耗时）。 | “在实验阶段，我设计了消融实验。大家看这组对比图，在相同的计算预算（如16ms或100 SPP）下，优化后的算法显著降低了噪点/提升了帧率。” (1 min) |
| Page 7 | 核心数据：收敛曲线 🌟<br>一张折线图：横轴SPP/时间，纵轴MSE/代价。两条线对比。 | “这是本次汇报最核心的数据。 从收敛曲线可以看出，红线（我的算法）比蓝线（Baseline）下降得更快。在达到相同MSE阈值时，我的算法节省了X%的迭代次数/计算时间。” (2 min) |
| Page 8 | 总结与 Trade-off<br>优点总结 + 局限性分析。 | “总结来说，我的算法成功优化了目标函数。但任何优化都有代价（Trade-off），比如增加了内存开销/预处理时间。未来可以考虑结合XX算法做进一步改进。我的汇报完毕，谢谢！” (1 min) |





| 难度排名 | 优化方向 | 最优化数学本质 | 为什么是这个难度？ (你的总结+补充) | 报告核心卖点 (怎么拿高分) 🌟 | 适合人群 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| Top 4<br>(最简单) | 基础方差优化<br>(分层采样 / 俄罗斯轮盘赌) | 截断误差与方差的权衡<br>用简单的概率分布替代均匀分布；无偏序贯决策。 | 代码极简。 只需修改随机数生成逻辑，加几行代码就能跑通。数学推导直观，不需要复杂的数据结构。 | 证明“无偏性”。 用数学公式推导证明俄罗斯轮盘赌不会改变期望；画出“固定深度截断(有偏)”与“轮盘赌(无偏)”的误差对比图。 | 编程基础薄弱，想快速搞定作业拿个及格/良好。 |
| Top 3<br>(中等) | 最优样本分配<br>(自适应采样) | 带约束的资源分配<br>(拉格朗日乘子法 / 凸优化) | 逻辑清晰但需维护额外缓存。 需要计算每个像素的方差，并根据方差动态分配计算预算。工程上容易实现，但需要写方差估计算法。 | 展示“资源分配热力图”。 画一张图，证明你的算法把 80% 的样本（计算资源）精准分配给了 20% 的高方差区域（如阴影边缘、焦散）。 | 喜欢数据分析，想画出漂亮的收敛曲线和资源分配图。 |
| Top 2<br>(偏难) | 空间搜索优化<br>(SAH-BVH 构建) | 离散组合优化<br>(贪心算法 / 启发式搜索) | 工程实现极其繁琐。 虽然数学公式（SAH）只有一个，但涉及树形数据结构、内存对齐、AABB 相交测试的浮点数精度问题（Bug极多，极难Debug）。 | 展示“复杂度缩放曲线”。 画出场景三角形数量  $ N $  从 1万 增加到 100万 时，渲染时间的折线图。证明你的算法把  $ O(N) $  降到了  $ O(\log N) $ 。 | C++/Python 数据结构功底扎实，有耐心 Debug，喜欢底层优化。 |
| Top 1<br>(最难) | 多重重要性采样<br>(MIS 权重优化) | 多目标混合分布优化<br>(启发式权重 / 方差最小化) | 数学推导反人类。 需要同时维护光源采样和 BRDF 采样的概率密度函数(PDF)，涉及测度转换。一旦公式写错，画面直接黑屏或满屏噪点，很难排查。 | 展示“消融实验 (Ablation Study)”。 对比“仅光源采样”、“仅BRDF采样”和“MIS”在特定材质（如高光泽金属）下的收敛速度，证明 MIS 是方差的“下界”。 | 数学极好，有志于图形学科研，追求学术深度。 |
| Top 0<br>(地狱级) | Metropolis 光传输<br>(MLT) *(补充)* | 高维非凸空间全局优化<br>(MCMC / 马尔可夫链) | 数学与工程的双重灾难。 需要设计变异策略、计算雅可比行列式、处理细致平衡条件。传统 MLT 无法并行，GPU 实现极难。 | 展示“状态转移轨迹”。 可视化马尔可夫链在复杂光路（如门缝漏光）中的“随机游走”过程，证明它能找到传统方法找不到的“全局最优解（高能量光路）”。 | 打算把大作业发顶会论文 (SIGGRAPH)，或者申请顶尖 PhD。 |


